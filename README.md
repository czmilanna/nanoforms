# nanoforms

### Requirements
* docker
* docker-compose
* linux
* make

### Deploy
Change `docker-compose.env` file and run below command:
```shell
make deploy
```

### Destroy
Run below command:
```shell
make deploy
```